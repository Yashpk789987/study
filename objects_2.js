const data = require("./data");

// Object Destructuring
const { age, college } = data;

console.log(age);
console.log(college);

const info = {
  i: 1,
  j: 2,
  k: 3,
};

// Object Destructing
const { i } = info;
console.log(i);
